# Flag Removal Report: ROUTINES_RUNS_V2_SUPPORTED / ROUTINES_EMAIL_TRIGGER_SUPPORTED

**Date:** 2026-07-21  
**Branch:** main  
**Commit:** d5c9a35  
**17 files changed, 41 insertions, 591 deletions**

---

## Files Deleted

- `lib/routine-runs/flags.ts` — canonical flag definitions; deleted entirely.

---

## Files Modified

### Schema / Flags

- `app/(application)/workflows/schema-flags.ts` — removed the two re-export lines for `ROUTINES_RUNS_V2_SUPPORTED` and `ROUTINES_EMAIL_TRIGGER_SUPPORTED`; stale comment block removed; remaining flags (RBAC_TEAMS, LAST_RUN_EMBEDDED, SCHEDULE_EMBEDDED, STATUS_FILTER, LAST_RUN_SORT, AGENTS_BATCH) untouched.

### Shell / Navigation

- `components/shell/nav-config.ts` — removed flag import; `runs` nav entry made a plain permanent member of `NAV_ENTRIES` (was a conditional spread).
- `components/shell/nav-config.test.ts` — removed `ROUTINES_RUNS_V2_SUPPORTED` import; `RUNS_ROWS` made unconditional `["runs"]`; `activeEntryFor("/runs")` test made unconditional. The pre-existing failing test (`agents:read → Build's read surfaces`) is unchanged beyond what the runs-entry change mechanically required — it was already failing before this work.
- `components/shell/use-runs-attention.ts` — `enabled` simplified to `!!user && can(user, { area: "workflows", level: "read" })`; flag import removed.

### Workflows Feature

- `app/(application)/workflows/[id]/sections/runs.tsx` — `RunsSection` dispatcher and `LegacyRunsSection` deleted; `RunsSection` is now the v2 implementation directly. The `onRetry` prop is kept in the interface with a comment explaining it is accepted-but-unused (compatibility with the `routine-workbench.tsx` call site; the v2 list manages its own retry flow).
- `app/(application)/workflows/[id]/sections/triggers.tsx` — flag import removed; `skip: !ROUTINES_EMAIL_TRIGGER_SUPPORTED` guards removed from both `useQuery` calls; `if (!ROUTINES_EMAIL_TRIGGER_SUPPORTED) return null` guard removed. Doc comment cleaned up.
- `app/(application)/workflows/[id]/components/routine-workbench.tsx` — flag import removed; `triggers` filter condition removed from `sectionIds` useMemo (only the `danger` canDelete gate remains).
- `app/(application)/workflows/queries.ts` — `GET_JOB_RESULTS_LIGHT` and `GET_JOB_RESULT_BY_ID` deleted (only consumer was `LegacyRunsSection`; verified via git grep — the home feature has its own local copy). Email trigger section comment cleaned up.

### Runs Page

- `app/(application)/runs/runs-client.tsx` — `ROUTINES_RUNS_V2_SUPPORTED` import and the flag-off `EmptyState` branch removed; real page always renders. Stale `Activity` import removed.
- `app/(application)/runs/layout.tsx` — stale flag reference in comment removed.

### Chat

- `app/(application)/chat/components/run-session-banner.tsx` — flag import removed; `enabled` simplified to `jobResultId !== null`.

### Configuration

- `app/(application)/configuration/components/email-intake-view.tsx` — flag import removed; `skip: !ROUTINES_EMAIL_TRIGGER_SUPPORTED` on the query removed; flag-off `EmptyState` branch removed. Now-unused `EmptyState` and `Mail` imports removed.

### i18n

- `messages/en.json` — removed `configuration.emailIntake.unavailableTitle`, `configuration.emailIntake.unavailableDescription`, `runs.unavailableTitle`, `runs.unavailableDescription`.
- `messages/de.json` — same 4 keys removed.

### Widget (comment-only)

- `components/widgets/routine-runs/runs-list.tsx` — stale doc-comment reference to `ROUTINES_RUNS_V2_SUPPORTED` removed.
- `lib/routine-runs/queries.ts` — stale flag references in doc comments removed.

---

## Query Constants Decision

`GET_JOB_RESULTS_LIGHT` and `GET_JOB_RESULT_BY_ID` in `app/(application)/workflows/queries.ts` were deleted. Verified via `git grep` that the only consumer was `LegacyRunsSection` in `runs.tsx`. The home feature (`app/(application)/(home)/`) has its own independent local copy in its own `queries.ts` and is unaffected.

## onRetry Prop Decision

Kept `onRetry?: (prefill: Record<string, string>) => void` in `RunsSectionProps` as an accepted-but-unused prop with a one-line comment. This avoids a change to `routine-workbench.tsx`'s call site and keeps the type signature stable. The v2 `RoutineRunsList` manages its own retry via `retryRoutineRun` mutation.

---

## Verification Results

| Check | Result |
|---|---|
| `git grep ROUTINES_RUNS_V2_SUPPORTED\|ROUTINES_EMAIL_TRIGGER_SUPPORTED` | **0 hits** |
| `npx vitest run` | **1 failure** — pre-existing `agents:read → Build's read surfaces` matrix test only |
| `npm run lint` | **1 error** — pre-existing `app/(application)/data` imports agents feature (`entity-types.tsx`); 0 new errors |
| `npm run check-messages` | **parity OK — 3606 keys across en, de** |
